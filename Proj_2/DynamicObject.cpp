#include "DynamicObject.h"

DynamicObject::DynamicObject(){
    // nothing to do
}

DynamicObject::~DynamicObject(){
    //nothing to do
}

void DynamicObject::create (struct MyMesh* mesh){}
void DynamicObject::draw(struct MyMesh* mesh, VSShaderLib& shader, GLint& pvm_uniformId, GLint& vm_uniformId, GLint& normal_uniformId, GLint& texMode_uniformId){}